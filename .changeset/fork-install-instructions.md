---
"@ai-hero/sandcastle": patch
---

Document fork-local install paths in the README. New "Installing this fork" section covers consumer install via `github:larmax82/sandcastle#<tag>`, the published tag scheme (`v0.5.7-cursor.0/.1/.2`), the development-clone workflow, and common troubleshooting (missing `dist/`, `tsgo` not found, lockfile pinning).
